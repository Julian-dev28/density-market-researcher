# =============================================================================
# Macro Research Pipeline — Environment Configuration
# Copy to .env and fill in your values.
# The pipeline runs with zero credentials in dry-run mode (uses fixture data).
# =============================================================================

# ---------------------------------------------------------------------------
# Postgres — Required only when DRY_RUN=false
# Example: postgres://postgres:postgres@localhost:5432/macro_research
# ---------------------------------------------------------------------------
DATABASE_URL=

# ---------------------------------------------------------------------------
# FRED API — Federal Reserve Economic Data
# Free key: https://fred.stlouisfed.org/docs/api/api_key.html
# Without a key, sample fixture data is used automatically.
# ---------------------------------------------------------------------------
FRED_API_KEY=

# ---------------------------------------------------------------------------
# CoinMarketCap API
# Free key: https://coinmarketcap.com/api/
# Without a key, sample fixture data is used automatically.
# ---------------------------------------------------------------------------
COINMARKETCAP_API_KEY=

# ---------------------------------------------------------------------------
# Anthropic — Claude Opus 4.6 for the research agent and reports
# Required for: npm run agent, npm run report
# Get a key: https://console.anthropic.com/
# ---------------------------------------------------------------------------
ANTHROPIC_API_KEY=

# ---------------------------------------------------------------------------
# Fireworks AI — Deepseek-v3.1-671B quality judge (CryptoAnalystBench)
# Used to score research notes on 4 dimensions — the actual model used by
# Sentient's CryptoAnalystBench evaluation framework.
# Falls back to Claude Haiku if not set.
# Get a key: https://fireworks.ai
# ---------------------------------------------------------------------------
FIREWORKS_API_KEY=

# ---------------------------------------------------------------------------
# OpenHands — AI software engineer for autonomous platform expansion
# When the research agent identifies a data gap, it tasks OpenHands to build
# the missing ingestion module and open a PR automatically.
# Get a key: https://app.all-hands.dev
# Optional: without this, the expand_data_sources tool returns a no-op.
# ---------------------------------------------------------------------------
OPENHANDS_API_KEY=
# GitHub repo for OpenHands to operate on (format: "owner/repo")
GITHUB_REPO=

# ---------------------------------------------------------------------------
# Trigger.dev — Scheduled pipeline runs + event-driven agent
# Create a project at https://trigger.dev/ and get your secret key
# ---------------------------------------------------------------------------
TRIGGER_SECRET_KEY=
TRIGGER_PROJECT_ID=

# ---------------------------------------------------------------------------
# Vercel — Required for GitHub Actions deploy workflow
# Get from https://vercel.com/account/tokens
# ---------------------------------------------------------------------------
VERCEL_TOKEN=
VERCEL_ORG_ID=
VERCEL_PROJECT_ID=

# ---------------------------------------------------------------------------
# Supabase — for the web frontend (web/.env.local)
# Get from https://supabase.com/dashboard/project/<ref>/settings/api
# ---------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ---------------------------------------------------------------------------
# x402 Payment Protocol — monetize the research feed with USDC micropayments
# https://github.com/sentient-agi/agent-payments-skill | https://x402.org
# Set PAYMENT_ENABLED=true to enforce 402 on /api/feed (default: false)
# ---------------------------------------------------------------------------
PAYMENT_ENABLED=false
X402_PAY_TO_ADDRESS=        # your wallet address receiving USDC
X402_PRICE_USDC_ATOMIC=1  # price in USDC atomic units (100000 = $0.10)
X402_NETWORK=base-sepolia   # base | base-sepolia | ethereum

# ---------------------------------------------------------------------------
# Pipeline behavior
# ---------------------------------------------------------------------------
MODE=all
LOG_LEVEL=info
BATCH_SIZE=100
FRED_LOOKBACK_OBSERVATIONS=53
